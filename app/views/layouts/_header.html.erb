    <nav class="navbar navbar-expand-lg bg-light mb-5 fs-6 p-2" data-bs-theme="light">
            <div class="container-fluid">
            <a class="navbar-brand fs-5" href="#">Kittenshop</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor03">
                <div class="">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link active" href="#">Home
                            <span class="visually-hidden">(current)</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">E-shop</a>
                        </li>
                    </ul>
                </div>
                <div>
                    <ul class="navbar-nav me-3">
                        <li class="nav-item ">
                            <a class="nav-link" href="#"><i class="fas fa-shopping-cart"></i> Basket</a>
                        </li>
                        <li class="nav-item dropdown">
                        <% if user_signed_in? %>
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">log out</a>
                            <div class="dropdown-menu">
                            
                                <!-- Le contenu à afficher lorsque la session est active -->
                                <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dropdown-item" %>

                            <% else %>
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Sign in / Sign up</a>
                            <div class="dropdown-menu">
                                <!-- Le contenu à afficher lorsque la session n'est pas active -->
                                        <a class="dropdown-item" href="<%= new_user_session_path %>">Sign in</a>
                                        <a class="dropdown-item" href="<%=  new_user_registration_path %>">Sign up</a>                                    
                            <% end %>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
            </div>
        </nav> 
